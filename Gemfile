# frozen_string_literal: true

source 'https://rubygems.org'

# Specify your gem's dependencies in turbo_test_static_analysis.gemspec
gemspec

eval_gemfile(File.expand_path('Gemfile.common', __dir__))

gem 'simplecov', '~> 0.18.5'
